# دي مكتبة مش هنحتاج نعمل import لأي حاجة فيها لأن بايثون بيطبع طبيعي بـ print

# دالة البداية الرئيسية
def selection_sort():
    arr = [64, 25, 12, 22, 11]  # مصفوفة فيها أرقام عشوائية
    n = len(arr)  # بنحسب عدد العناصر في المصفوفة

    # اللوب الخارجي بيمشي على كل عنصر لحد ما نوصل قبل الأخير
    for i in range(n - 1):
        min_index = i  # بنفترض إن أول عنصر في الجزء ده هو الأصغر

        # اللوب الداخلي بيدور على الأصغر في باقي العناصر
        for j in range(i + 1, n):
            if arr[j] < arr[min_index]:  # لو لقينا رقم أصغر من الحالي
                min_index = j  # نحفظ مكانه علشان نبدله بعد كده

        # بنبدل بين العنصر الأصغر والعنصر الحالي
        arr[i], arr[min_index] = arr[min_index], arr[i]

    print("Sorted array:", arr)  # في الآخر بنطبع المصفوفة بعد الترتيب


# هنا بننادي على الدالة عشان تشتغل
selection_sort()
